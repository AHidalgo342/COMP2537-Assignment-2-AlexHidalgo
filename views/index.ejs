<%- include('templates/header') %>

<div class="px-4 py-5 my-5 text-center"> 
    <h1 class="display-5 fw-bold text-body-emphasis">Welcome<% if (authenticated) { %> <%= user %><% } %>!</h1>
        <div class="col-lg-6 mx-auto">
            <p class="lead mb-4">This is just some sample text that i'll definitely replace in the final product, right?</p>
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center"> 
                <% if (authenticated) { %>
                    <a class="btn btn-primary btn-lg px-4 gap-3" href="/members" role="button">Go to Members Area</a>
                    <a class="btn btn-primary btn-lg px-4 gap-3" href="/logout" role="button">Logout</a>
                <% } else { %>
                    <a class="btn btn-primary btn-lg px-4 gap-3" href="/signUp" role="button">Sign Up</a>
                    <a class="btn btn-primary btn-lg px-4 gap-3" href="/login" role="button">Login</a>
                <% } %>
            </div>
        </div>
</div>

<%- include('templates/footer') %>